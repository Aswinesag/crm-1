import PRStatusBadge from "./PRStatusBadge";
const PRTable = ({ prs = [], onStatus, onRFQ }) => <div className="overflow-x-auto bg-base-100 rounded-xl shadow"><table className="table table-zebra"><thead><tr><th>PR</th><th>Items</th><th>Department</th><th>Status</th><th>Actions</th></tr></thead><tbody>{prs.map((pr) => <tr key={pr._id}><td>{pr.requisitionNo}</td><td>{pr.items?.map((line) => `${line.itemNameSnapshot} (${line.requestedQuantity})`).join(", ") || "Legacy/unresolved"}</td><td>{pr.department}</td><td><PRStatusBadge status={pr.status}/></td><td className="flex gap-2">{pr.status === "PENDING" && <><button className="btn btn-success btn-xs" onClick={() => onStatus(pr._id, "APPROVED")}>Approve</button><button className="btn btn-error btn-outline btn-xs" onClick={() => onStatus(pr._id, "REJECTED")}>Reject</button></>}{pr.status === "APPROVED" && <button className="btn btn-primary btn-xs" onClick={() => onRFQ(pr._id)}>Create RFQ</button>}</td></tr>)}</tbody></table></div>;
export default PRTable;
